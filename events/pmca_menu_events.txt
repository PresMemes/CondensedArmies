namespace = pmca_menu_events

event = {
	id = pmca_menu_events.42
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		set_global_flag = condensed_armies_installed
		if = {
			limit = {
				NOR = {
					has_global_flag = pmca_ai_allowed
					has_global_flag = pmca_ai_banned
				}
			}
			set_global_flag = pmca_ai_allowed
		}
	}
}

country_event = {
	id = pmca_menu_events.1
	title = pmca_menu_events.1.title
	desc = pmca_menu_events.1.desc
	is_triggered_only = yes

	picture = GFX_evt_interior_battle
	show_sound = event_space_battle

	option = {
		name = pmca_menu_events.1.disable
		trigger = { has_global_flag = pmca_ai_allowed }
		hidden_effect = {
			remove_global_flag = pmca_ai_allowed
			set_global_flag = pmca_ai_banned
			country_event = { id = pmca_menu_events.1 }
		}
	}

	option = {
		name = pmca_menu_events.1.enable
		trigger = { has_global_flag =  pmca_ai_banned }
		hidden_effect = {
			set_global_flag = pmca_ai_allowed
			remove_global_flag = pmca_ai_banned
			country_event = { id = pmca_menu_events.1 }
		}
	}

	option = {
		name = EXIT
	}
}
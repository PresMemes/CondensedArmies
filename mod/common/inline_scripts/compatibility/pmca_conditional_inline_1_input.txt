# Dear Irony please fallback to simple parser
# Credit to TTFTCUTS and MrRoAdd for letting their intrusive thoughts win
# So the weird ass equation below is this:
# |(((x / x) - 1) / ((0/0) - 1)) - 1|
# Where X is $MOD_VARIABLE_1$ and will always return a 0 or a 1, which is needed for the conditional inline to work
# If $MOD_VARIABLE_1 == 0, return 0
# If $MOD_VARIABLE_1 != 0, return 1

inline_script = {
	script = compatibility/pmca_conditional_inline_step_2

	CASE = @[ (|(((($MOD_VARIABLE_1$ / $MOD_VARIABLE_1$) - 1) / ((0 / 0) - 1)) - 1)|) ]
	CONTENT = "$CONTENT$"
}